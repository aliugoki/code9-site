# Copy to .env and fill in real values. .env is git-ignored (holds secrets).
MYSQL_DATABASE=u293320349_mE32w
MYSQL_USER=u293320349_cQUK9
MYSQL_PASSWORD=__SET_ME__
MYSQL_ROOT_PASSWORD=__SET_ME__

# Host port the site is served on
HTTP_PORT=8082

# URL the site answers on locally
SITE_URL=http://localhost:8082

# Image versions — pinned for reproducibility
MARIADB_VERSION=11.8
NGINX_VERSION=stable-alpine
PHP_WORDPRESS_VERSION=php8.2-fpm
REDIS_VERSION=7-alpine
